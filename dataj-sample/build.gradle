apply plugin: 'idea'

dependencies {
    testCompile 'junit:junit:4.12'
}

sourceSets.main.java.srcDir new File(projectDir, 'generated')
idea {
    module {
        generatedSourceDirs += file(projectDir.toString() + '/generated')
    }
}